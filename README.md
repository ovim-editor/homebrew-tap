# homebrew-tap

Homebrew tap for [ovim](https://github.com/ovim-editor/ovim) — Oxidized Vim.

## Install

```bash
brew install ovim-editor/tap/ovim
```

Or add the tap first, then install:

```bash
brew tap ovim-editor/tap
brew install ovim
```

## Update

```bash
brew update
brew upgrade ovim
```
